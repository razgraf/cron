/** Maximum output tokens per AI run. Controls cost per task execution. */
export const MAX_OUTPUT_TOKENS = 4096;

/** Default model used for task runs. */
export const DEFAULT_MODEL = "claude-sonnet-4-20250514";
